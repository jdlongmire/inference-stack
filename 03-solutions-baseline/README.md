# 03-solutions-baseline/

**The built thing.** Source, content, or artifacts, depending on the repo class.

Sub-number as the repo class requires (`3.1-`, `3.2-`, …) rather than inventing a parallel
top-level scheme — see `profiles/` for a class that does this.
