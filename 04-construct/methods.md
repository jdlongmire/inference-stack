# Methods — tradecraft

> Template stub. Replace this with the repo's actual content, or delete the file if the surface
> genuinely does not apply — an empty governance surface is worse than an absent one.

Working rules. Each one should name the specific failure that caused it; a rule with no failure behind it is decoration.
